To build simply run:
```bash
./build
./build install --prefix PREFIX --asgs-home ASGS_HOME
```
The install step is required. A module file is generated that has env variables needed for the gui to function.
